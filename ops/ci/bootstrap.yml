# <<AC_ANCHOR|LOG|CI|bootstrap-pipeline@v1>>
# Placeholder GitHub Actions workflow definition.
# Replace `TODO` sections once implementation artifacts exist.
name: bootstrap-ci
on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
jobs:
  lint-and-docs:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.11'
      - name: Install tooling
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt || echo "TODO: define requirements"
      - name: Static checks
        run: |
          make lint || echo "TODO: implement lint target"
          make docs-check || echo "TODO: implement docs-check target"
  tests:
    runs-on: ubuntu-latest
    needs: lint-and-docs
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.11'
      - name: Install tooling
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt || echo "TODO: define requirements"
      - name: Run tests
        run: |
          make test || echo "TODO: implement test target"
